type: object
additionalProperties:
  type: object
  properties:
    state:
      type: object
      additionalProperties:
        type: object
        properties:
          type:
            type: string
    actions:
      type: object
      description: Actions that change the reducer's state.
      additionalProperties:
        type: object
    name:
      type: string
      description: The name of the reducer.
  description: The name of the reducer.
